[build]
  command = "npm run build"
  publish = "dist"

# IMPORTANT: replace this with your real Render backend URL once it's deployed
# (e.g. https://agro-backend.onrender.com)
[[redirects]]
  from = "/api/*"
  to = "https://REPLACE_WITH_BACKEND_URL/:splat"
  status = 200
  force = true

[[redirects]]
  from = "/uploads/*"
  to = "https://REPLACE_WITH_BACKEND_URL/uploads/:splat"
  status = 200
  force = true

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
